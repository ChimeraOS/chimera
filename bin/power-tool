#! /bin/bash

if [ "${1}" == "get-tdp" ]; then
	ryzenadj --info | grep stapm-limit | cut -d '|' -f 3 | tr -d ' ' | cut -d '.' -f 1
fi

if [ "${1}" == "set-tdp" ]; then
	ryzenadj --stapm-limit=${2}000 &> /dev/null
fi
